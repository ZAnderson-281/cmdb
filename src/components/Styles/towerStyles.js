import { makeStyles } from "@material-ui/core";

export const towerStyles = makeStyles((theme) => ({
  cardTower: {
    height: "100%",
    width: "30%",
    padding: ".5rem",
    background: "none",
  },
  card: {
    margin: "1rem .5rem",
  },
}));
