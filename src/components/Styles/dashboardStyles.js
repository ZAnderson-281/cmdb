import { makeStyles } from "@material-ui/core";

export const dashboardStyles = makeStyles((theme) => ({
  headContainer: {
    marginTop: "5rem",
    marginLeft: "5rem",
    marginRight: "5rem",
    marginBottom: "1rem",
    padding: "1rem",
    height: "fit-content",
    backgroundColor: "red",
  },
  bodyContainer: {
    margin: "1rem 5rem",
    padding: "1rem",
    height: "fit-content",
    backgroundColor: "red",
  },
}));
